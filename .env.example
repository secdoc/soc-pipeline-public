# Copy to .env and fill in. NEVER commit the real .env.
GMP_USER=<GMP_USER>
GMP_PASS=<GMP_PASS>
SCANNER_HOST=<SCANNER_HOST>       # Greenbone Docker host (SSH for deploy)
GRAYLOG_HOST=<GRAYLOG_HOST>      # GELF input target
GRAYLOG_GELF_PORT=12201
WAZUH_HOST=<WAZUH_HOST>
# Optional AI enrichment path:
ANTHROPIC_API_KEY=<API_KEY>
